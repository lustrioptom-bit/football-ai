# render.yaml
services:
  - type: web
    name: football-ai-web
    env: python
    startCommand: cd web && streamlit run app.py --server.port $PORT
    envVars:
      - key: PORT
        value: 10000
